//mui imports
import { Box } from "@mui/system";

const NotFoundPage = () => (
  <Box
    component="img"
    sx={{
      height: '100vh',
      width: '100%'
    }}
    alt="web Not found page image"
    src="notFound.jpg"
  />
);

export default NotFoundPage;
